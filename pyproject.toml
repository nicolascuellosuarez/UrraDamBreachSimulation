[project]
name = "urradambreachsimulation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.8",
    "plotly>=6.7.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "radon>=6.0.1",
]
